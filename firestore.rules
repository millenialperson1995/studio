/**
 * @file This Firestore Security Rules file enforces a simple ownership-based model.
 *
 * Core Philosophy:
 *   - All authenticated users can read and write to most collections.
 *   - This is a simplified ruleset for initial development and should be
 *     tightened before production.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Checks if the user is signed in.
     */
    function isSignedIn() {
      return request.auth != null;
    }
    
    // Default-deny all reads/writes and open up collections one by one.
    
    match /{document=**} {
      allow read, write: if isSignedIn();
    }
  }
}
